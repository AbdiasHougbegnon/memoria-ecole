<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.memoria</groupId>
    <artifactId>memoria-core</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>memoria-core</name>
    <description>Noyau moteur Memoria - Phase 1, Jalon 1</description>

    <properties>
        <java.version>21</java.version>
        <!-- Couverture de lignes reellement mesuree (mvn verify, 23/07/2026) : 52,6%
             (1268/2412 lignes). Seuil fixe legerement en dessous (baseline honnete,
             pas invente) : toute regression future casse la CI, mais une petite
             marge absorbe les variations mineures entre runs. -->
        <jacoco.line.coverage.minimum>0.50</jacoco.line.coverage.minimum>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- @SuppressFBWarnings uniquement : justifie une suppression FindSecBugs
             dans le code au lieu d'un filtre d'exclusion externe et silencieux. -->
        <dependency>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-annotations</artifactId>
            <version>4.10.3</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <!-- Gates de qualite (brique "phase-8-gates-qualite") : "SonarQube ou
                 equivalent" du master prompt, sans compte externe. Couverture
                 (JaCoCo) + analyse statique/OWASP (SpotBugs + FindSecBugs) ici ; le
                 scan des dependances (CVE) se fait au niveau CI, pas Maven, voir le
                 commentaire plus bas et .github/workflows/ci.yml. -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.line.coverage.minimum}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- threshold=High : ne bloque que les findings severes (bruit sinon).
                 N'analyse que les classes main par defaut (jamais les tests). -->
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>4.10.3.0</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>High</threshold>
                    <plugins>
                        <plugin>
                            <groupId>com.h3xstream.findsecbugs</groupId>
                            <artifactId>findsecbugs-plugin</artifactId>
                            <version>1.14.0</version>
                        </plugin>
                    </plugins>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <phase>verify</phase>
                    </execution>
                </executions>
            </plugin>

            <!-- Le scan des dependances Java (CVE) n'est PAS fait ici par un plugin
                 Maven, deux tentatives reelles ont echoue :
                 1) OWASP Dependency-Check (base NVD locale) : synchroniser les
                    370 000+ enregistrements depuis zero depasse largement ce qui
                    est raisonnable sur un runner GitHub Actions ephemere, meme
                    avec une cle NVD_API_KEY (confirme a plusieurs reprises, local
                    ET CI).
                 2) Sonatype OSS Index (ossindex-maven-plugin) : l'API anonyme
                    renvoie desormais 401 Unauthorized (compte requis depuis un
                    changement de politique Sonatype), et pire, le plugin avale
                    cette erreur en simple WARNING et rapporte BUILD SUCCESS sans
                    avoir verifie la moindre dependance : un faux sentiment de
                    securite, pas juste un outil plus lent.
                 Le scan reel se fait via "GitHub Dependency Review"
                 (.github/workflows/ci.yml, job "dependency-review") : natif a
                 GitHub, zero compte externe, bloque une PR qui introduit une
                 dependance a vulnerabilite connue, correspond exactement au
                 gate du master prompt ("aucune PR n'est fusionnee sans scan des
                 dependances"). -->
        </plugins>
    </build>

</project>
